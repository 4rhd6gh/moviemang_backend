pipeline {
    agent any

    environment{
        GIT_COMMIT_HASH = sh (script: "git log -n 1 --pretty=format:'%H'", returnStdout: true)
        GIT_COMMIT_TAG = sh "echo $GIT_COMMIT_HASH | cut -c -8"
    }
    
    

    
    stages {
        stage('Build') {
        steps {
            sh "echo Git commit hash: ${GIT_COMMIT_HASH}"
            sh "echo Git commit tag: ${GIT_COMMIT_TAG}"
            sh 'echo "Hello World"'
        }
        }
    }
}